NODE_ENV=production
# Optional: leave empty to auto-use the current site origin.
NEXT_PUBLIC_API_BASE_URL=
# Optional hard fallback for middleware redirects (avoid localhost redirects behind proxies).
NEXT_PUBLIC_APP_ORIGIN=https://onlinedrawinggame.online
NEXT_PUBLIC_API_TIMEOUT_MS=10000
NEXT_PUBLIC_API_RETRY_COUNT=1
NEXT_PUBLIC_ACCESS_COOKIE_NAME=access_token
